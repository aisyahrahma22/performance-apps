export enum LearningEvalStatusEnum {
  AVAILABLE = 'AVAILABLE',
  DRAFT = 'DRAFT',
  PUBLISHED = 'PUBLISHED',
}

export enum LEPeriodicIntervalUnitEnum {
  SECONDS = 's',
  MINUTES = 'm',
  HOURS = 'H',
  DAYS = 'd',
  WEEKS = 'w',
  MONTHS = 'M',
  YEARS = 'y',
}
